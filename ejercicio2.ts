// Función para elevar un numero a la potencia dada
function elevar(base: number, exponente: number) : number {
    return Math.pow(base, exponente)
}

// Prueba de la función
console.log("La base 8 elevado al cuadrado es " + elevar(8, 2))